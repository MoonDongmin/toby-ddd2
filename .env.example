# PostgreSQL (앱 접속 정보 + docker Postgres 컨테이너 초기화에 공유됨)
POSTGRES_USER=hyeonsig
POSTGRES_PASSWORD=change-me
POSTGRES_DB=hyeonsig

# 호스트에 노출할 DB 포트 (TypeORM 접속 포트 + docker "${DB_PORT}:5432" 매핑)
DB_PORT=5432

# NestJS 앱 포트 (선택 — 미설정 시 3000)
PORT=3000
